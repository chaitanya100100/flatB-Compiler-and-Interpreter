declblock{
    int first, second[20];
    int third;
    ;
}

codeblock{
    54;
    ;
    54 + 23 * 84 - (-c / -65 * 84 - (54 / dd) / (95 * 21) * -(in / -45));
    ;
    54 * d + i5k[50];
    sdf = df[84] = d = df = a[2] * b[2] / 56 * (c - d[3] * (x) / (-df[20]));
}
